# Public API — `bofa-design-system`

Generated by `tools/api-surface.mjs` from the built `.d.ts`. Do not edit by hand.

This is the surface downstream consumers compile and template against. **Any diff here is a
breaking-change conversation**, not an implementation detail — the migration is supposed to
absorb Material's churn so that this file does not move.

## Components

### `<bds-alert-banner>` — `BdsAlertBannerComponent`

- **Inputs:** `dismissible`, `severity`
- **Outputs:** _none_
- **Content slots:** `*`
- **Standalone:** no

```ts
dismissible: boolean;
severity: BdsAlertSeverity;
```

### `<bds-button>` — `BdsButtonComponent`

- **Inputs:** `disabled`, `loading`, `variant`
- **Outputs:** `bdsClick`
- **Content slots:** `*`, `*`, `*`
- **Standalone:** no

```ts
bdsClick: EventEmitter<MouseEvent>;
disabled: boolean;
loading: boolean;
onClick(event: MouseEvent): void;
variant: BdsButtonVariant;
```

### `<bds-card>` — `BdsCardComponent`

- **Inputs:** `elevated`, `title`
- **Outputs:** _none_
- **Content slots:** `*`
- **Standalone:** no

```ts
elevated: boolean;
title: string;
```

## Modules

### `BofaDesignSystemModule` — NgModule

- **Declares:** `BdsAlertBannerComponent`, `BdsButtonComponent`, `BdsCardComponent`
- **Imports:** `CommonModule`, `MatCardModule`, `MatIconModule`, `MatLegacyButtonModule`
- **Exports:** `BdsAlertBannerComponent`, `BdsButtonComponent`, `BdsCardComponent`

## Types and constants

### `BDS_COLOR_TOKENS`

```ts
BDS_COLOR_TOKENS: { readonly brandPrimary: "#012169"; readonly brandAccent: "#E31837"; readonly surface: "#FFFFFF"; readonly surfaceMuted: "#F4F6F8"; readonly textPrimary: "#0B1B32"; readonly textInverse: "#FFFFFF"; readonly success: "#1E7A34"; readonly warning: "#B26A00"; readonly danger: "#B3261E"; }
```

### `BDS_RADIUS_TOKENS`

```ts
BDS_RADIUS_TOKENS: { readonly sm: "4px"; readonly md: "8px"; readonly pill: "999px"; }
```

### `BDS_SPACING_TOKENS`

```ts
BDS_SPACING_TOKENS: { readonly xs: "4px"; readonly sm: "8px"; readonly md: "16px"; readonly lg: "24px"; readonly xl: "32px"; }
```

### `BdsAlertSeverity`

```ts
export type BdsAlertSeverity = 'info' | 'success' | 'warning' | 'danger';
```

### `BdsButtonVariant`

```ts
export type BdsButtonVariant = 'primary' | 'secondary' | 'danger';
```
